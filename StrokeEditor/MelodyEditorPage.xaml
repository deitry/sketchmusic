<Page
    x:Class="StrokeEditor.MelodyEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StrokeEditor"
    xmlns:sm="using:SketchMusic"
    xmlns:sf="using:SketchMusic.SFReader"
    xmlns:smv="using:SketchMusic.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <sm:InstrumentToTextConverter x:Key="instr2text"/>
            <DataTemplate x:Key="myResourceTemplate" x:DataType="sf:SFPreset">
                <TextBlock Text="{x:Bind name}"/>
            </DataTemplate>
            
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../SketchMusic/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <StackPanel x:Name="grid" 
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                BorderThickness="5"
                BorderBrush="Black"
                RenderTransformOrigin="0,1"
                Orientation="Horizontal"
                MaxWidth="1280">

        <StackPanel x:Name="HelpfulBar"
                    Orientation="Vertical"
                    BorderThickness="5"
                    BorderBrush="Black"
                    Width="40">
            <Button Name="GoBackBtn" Content="Вернуться назад" Click="GoBackBtn_Click" Padding="0" Width="60" Height="90"/>
            
            <Button Content="Texts"
                    Padding="0"
                    Height="90">
                <Button.Flyout>
                    <Flyout x:Name="TextsFlyout">
                        <ListView ItemsSource="{Binding}" x:Name="TextsList" SelectionChanged="ListView_SelectionChanged" Width="auto">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1" BorderBrush="AliceBlue">
                                        <TextBlock FontSize="14" Foreground="{ThemeResource SystemControlBackgroundBaseHighBrush}">
                                            <Run Text="{Binding instrument, Converter={StaticResource instr2text}}"/>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Flyout>
                </Button.Flyout>
            </Button>

            <Button Content="Add"
                    Padding="0"
                    Height="90">
                <Button.Flyout>
                    <Flyout x:Name="InstrumentsFlyout">
                        <ListView x:Name="InstrumentList"
                                  ItemsSource="{Binding}"
                                  IsItemClickEnabled="True"
                                  SelectionMode="None"
                                  ItemClick="ListView_ItemClick"
                                  Width="auto">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1" BorderBrush="AliceBlue">
                                        <TextBlock FontSize="14" Foreground="{ThemeResource SystemControlBackgroundBaseHighBrush}">
                                            <Run Text="{Binding _name}"/>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Flyout>
                </Button.Flyout>
            </Button>

            <Button Content="Delete"
                    Padding="0"
                    Height="90">
                <Button.Flyout>
                    <Flyout x:Name="DeleteTextFlyout">
                        <Button x:Name="DeleteTextBtn" Content="Ок" Click="DeleteTextBtn_Click"/>
                    </Flyout>
                </Button.Flyout>
            </Button>

        </StackPanel>

        <StackPanel
                    BorderThickness="5"
                    BorderBrush="Black" Width="950">
            <smv:TextRow x:Name="_textRow"
                         BorderThickness="2"
                         BorderBrush="White"
                         Height="180"
                         HorizontalAlignment="Left"
                         Margin="10,40,10,10"/>
            
            <smv:GenericKeyboard x:Name="_keyboard"
                                 BorderBrush="OrangeRed"
                                 BorderThickness="1"
                                 Background="Orange"
                                 Width="auto"
                                 VerticalAlignment="Bottom"
                                 HorizontalAlignment="Left"
                                 Style="{StaticResource GenericKeyboardStyle}"
                                         />
            
            <StackPanel Orientation="Vertical"
                        VerticalAlignment="Bottom">
                <TextBlock x:Name="CurPos">
                        Здесь будет отображаться текущая позиция курсора.
                </TextBlock>
                <TextBlock x:Name="BPMText">
                        Здесь будет отображаться текущее значение текста.
                </TextBlock>
                <!--<TextBlock x:Name="RealPos">
                        Здесь будет отображаться текущая позиция курсора.
                </TextBlock>
                <TextBlock x:Name="Version">
                        Версия 2016.12.16-17.
                </TextBlock>-->
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
