//
// BeatCanvas.h
// Объявление класса BeatCanvas.
//

#pragma once

#include "../SketchMusic.h"
#include "../base/Cursor.h"

// не нужен
/*public ref class SketchMusic::View::TextMultiRow sealed : public Windows::UI::Xaml::Controls::Control
{
private:
	// самый большой вопрос - в каком виде держать текст?
	// его стоит разбивать на много маленьких и каждой строке давать свой кусочек
	// или так и хранить в едином большом виде и каждая строка берёт только нужную ей часть
	// Второй вариант на данный момент предпочтительнее, но сложнее
	SketchMusic::Text^ _text;
	SketchMusic::Cursor^ _current;

	// стоит ли делать этот класс композицией TextRow или сделать его самостоятельным?

public:
	TextMultiRow();

	void SetScale(float scaleFactor) {}
	
};*/

